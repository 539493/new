services:
  - type: web
    name: tutoring-platform-am88
    env: node
    buildCommand: ./build.sh
    startCommand: cd backend && node production-server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 