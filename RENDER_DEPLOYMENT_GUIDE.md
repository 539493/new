# 🚀 Руководство по развертыванию на Render

## 📋 Предварительные требования

1. **Аккаунт на Render.com** - зарегистрируйтесь на [render.com](https://render.com)
2. **GitHub репозиторий** - проект должен быть загружен на GitHub
3. **Node.js 20+** - убедитесь, что в `package.json` указана версия Node.js >= 20.0.0

## 🔧 Шаг 1: Подготовка проекта

### Проверьте наличие файлов:
- ✅ `render.yaml` - конфигурация для Render
- ✅ `package.json` - с правильными скриптами
- ✅ `backend/production-server.cjs` - production сервер
- ✅ `src/config.ts` - конфигурация для production

### Убедитесь, что в `package.json` есть скрипты:
```json
{
  "scripts": {
    "build": "vite build",
    "start": "node backend/production-server.cjs"
  }
}
```

## 🌐 Шаг 2: Создание сервиса на Render

### 2.1 Войдите в Render Dashboard
1. Перейдите на [dashboard.render.com](https://dashboard.render.com)
2. Войдите в свой аккаунт

### 2.2 Создайте новый Web Service
1. Нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите ваш GitHub репозиторий

### 2.3 Настройте сервис
```
Name: tutoring-platform
Environment: Node
Region: Oregon (или ближайший к вам)
Branch: main
Root Directory: (оставьте пустым)
Build Command: npm install && npm run build
Start Command: npm start
```

### 2.4 Переменные окружения
Добавьте следующие переменные:
```
NODE_ENV=production
PORT=10000
HOST=0.0.0.0
```

## 🔄 Шаг 3: Автоматическое развертывание

### 3.1 Настройте auto-deploy
- ✅ **Auto-Deploy**: включено
- ✅ **Branch**: main
- ✅ **Health Check Path**: /

### 3.2 Первое развертывание
1. Нажмите **"Create Web Service"**
2. Render автоматически начнет сборку и развертывание
3. Дождитесь завершения (обычно 5-10 минут)

## 📊 Шаг 4: Мониторинг развертывания

### 4.1 Проверьте логи
1. Перейдите в раздел **"Logs"**
2. Убедитесь, что нет ошибок
3. Проверьте, что сервер запустился на порту 10000

### 4.2 Проверьте health check
1. Перейдите в раздел **"Health"**
2. Убедитесь, что статус **"Healthy"**

## 🌍 Шаг 5: Настройка домена

### 5.1 Получите URL
После успешного развертывания вы получите URL вида:
```
https://tutoring-platform-xxxxx.onrender.com
```

### 5.2 Обновите конфигурацию (если нужно)
Если нужно изменить URL в `src/config.ts`:
```typescript
// В продакшене используем внешний сервер
if (import.meta.env.PROD) {
  return 'https://your-app-name.onrender.com';
}
```

## 🔧 Шаг 6: Тестирование

### 6.1 Проверьте API endpoints
```
GET https://your-app.onrender.com/api/users
GET https://your-app.onrender.com/api/teachers
```

### 6.2 Проверьте WebSocket соединение
Откройте консоль браузера и проверьте:
```javascript
// Должно подключиться без ошибок
const socket = io('https://your-app.onrender.com');
```

### 6.3 Проверьте основную функциональность
1. Регистрация пользователей
2. Создание слотов репетиторами
3. Отображение репетиторов у учеников
4. Бронирование уроков

## 🚨 Устранение проблем

### Проблема: Build failed
**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что все зависимости указаны в `package.json`
3. Проверьте синтаксис кода

### Проблема: Server not starting
**Решение:**
1. Проверьте, что `startCommand` правильный
2. Убедитесь, что порт 10000 доступен
3. Проверьте логи сервера

### Проблема: CORS errors
**Решение:**
1. Проверьте настройки CORS в `production-server.cjs`
2. Убедитесь, что домен добавлен в `allowedOrigins`

### Проблема: WebSocket not connecting
**Решение:**
1. Проверьте настройки Socket.IO
2. Убедитесь, что WebSocket URL правильный
3. Проверьте логи сервера

## 📈 Шаг 7: Оптимизация

### 7.1 Настройте мониторинг
- Включите **"Logs"** для отслеживания ошибок
- Настройте **"Alerts"** для уведомлений

### 7.2 Оптимизируйте производительность
- Включите **"Auto-Scaling"** если нужно
- Настройте **"Health Checks"** для автоматического перезапуска

## 🔄 Шаг 8: Обновления

### 8.1 Автоматические обновления
При каждом push в ветку `main` Render автоматически:
1. Получит изменения
2. Пересоберет проект
3. Перезапустит сервер

### 8.2 Ручные обновления
1. Перейдите в **"Manual Deploy"**
2. Выберите нужную ветку
3. Нажмите **"Deploy latest commit"**

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что все файлы загружены в GitHub
3. Проверьте конфигурацию в `render.yaml`

## 🎉 Готово!

После успешного развертывания ваш проект будет доступен по URL:
```
https://your-app-name.onrender.com
```

Все функции должны работать:
- ✅ Регистрация пользователей
- ✅ Создание слотов репетиторами
- ✅ Отображение репетиторов у учеников
- ✅ Бронирование уроков
- ✅ Чат между пользователями
- ✅ Уведомления в реальном времени
